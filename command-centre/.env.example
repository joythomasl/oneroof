# Optional real incident feed. Leave unset to use the bundled offline demo data.
# Response shape: Incident[] or { "incidents": Incident[] }
VITE_INCIDENTS_API_URL=https://api.example.gov.in/v1/incidents
VITE_INCIDENTS_POLL_MS=5000
